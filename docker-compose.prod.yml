version: '3'
services:
  web:
    restart: always
  cache:
    restart: always
    networks:
      - default
      - ingress
  cron:
    restart: always
networks:
  ingress:
    external:
      name: ingress
